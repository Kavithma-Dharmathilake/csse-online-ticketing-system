class Bus {
    constructor( id, licenceNo, capacity, plateNo, currentPassenger ) {
        this.id = id,
        this.licenceNo = licenceNo,
        this.capacity = capacity,
        this.plateNo = plateNo,
        this.currentPassenger =  currentPassenger
    }
}

module.exports = Bus;