class AllTrips {
    constructor( id, name, time, start, end, fare ) {
        this.id = id;
       this.name = name;
       this.time=time;
       this.start = start;
       this.end=end;
       this.fare = fare;
    }
}

module.exports = AllTrips;